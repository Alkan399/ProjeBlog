@addTagHelper *,Microsoft.AspNetCore.Mvc.TagHelpers
@using ProjeBlog.Models;
@model List<ProjeBlog.Models.Content>
@{ 
    string sString = "";
}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">

<div class="container">
    <div>
        <div class="row">
            <div class="col-lg-10">

            </div>
            <div class="col-lg-2">
                <a class="btn btn-primary" asp-controller="Blog" asp-action="Create">Yeni</a>
            </div>
        </div>
    </div>
    @*<div>
        @for (int i = 0; i < Model.Count; i++)
        {
            <div class="row">
                <div class="card" style="width:60%" >

                    <div class="row w-100" style="margin-left:10px">
                        <form asp-controller="Blog" asp-action="ContentPage" method="post" id=@Model[i].Title asp-route-idx="@Model[i].ID">
                            @{
                                var methods = new Methods();
                                string submitTitleId = Model[i].ID.ToString() + "submitTitleId";
                                sString = methods.BlogContentFormatting(Model[i]) + "...";
                            }

                            <input class="h2" type="submit" name="submit" id="@submitTitleId" hidden="hidden" value=@Model[i].Title />
                            <a href="#" onclick="document.getElementById('@submitTitleId').click()" style="text-transform:capitalize;text-decoration-thickness:5px;font-size:24px;">@Model[i].Title</a>
                        </form>
                    </div>
                    <div class="row w-100" style="margin-left:12px">
                        <div class="col-lg-10 col-sm-12">
                            <p>
                                @Html.Raw(sString)
                            </p>
                        </div>
                        <div class="col-lg-2 col-sm-12">
                            <a class="icon row" asp-controller="Blog" asp-action="Update" asp-route-id="@Model[i].ID">
                                <i class="bi bi-pen"><label style="font-family: Arial; font-weight: bold;">Düzenle</label></i>
                            </a>
                            <a class="icon row" asp-controller="Blog" asp-action="Delete" asp-route-id="@Model[i].ID">
                                <i class="bi bi-trash"><label style="font-family:Arial;font-weight:bold;">Sil</label></i>
                            </a>
                        </div>
                        
                    </div>

                </div>
                
            </div>

         }
    </div>*@
    <div>
        @for (int i = 0; i < Model.Count; i++)
        {
            
            var methods = new Methods();
            string submitTitleId = Model[i].ID.ToString() + "submitTitleId";
            sString = methods.BlogContentFormatting(Model[i]) + "...";
             

            <div class="about_section layout_padding">
                <div class="container">
                    <div class="row">
                        <div class="col-lg-8 col-sm-12">
                            <div class="about_img"><img src="@Model[i].CoverImagePath"></div>
                            @*<div class="like_icon"><img src="images/like-icon.png"></div>*@
                            <p class="post_text">@Model[i].CreatedDate</p>
                            <h2 class="most_text">@Model[i].Title</h2>
                            <p class="lorem_text">@Html.Raw(sString)</p>
                            <div class="social_icon_main">
                                <div class="social_icon">
                                    <ul>
                                        <li><a href="#"><img src="/images/fb-icon.png"></a></li>
                                        <li><a href="#"><img src="/images/twitter-icon.png"></a></li>
                                        <li><a href="#"><img src="/images/instagram-icon.png"></a></li>
                                    </ul>
                                </div>
                                <div class="read_bt"><a asp-controller="Blog" asp-action="ContentPage" asp-route-idx="@Model[i].ID">Okumaya Devam Et</a></div>
                            </div>
                        </div>
@*                        <div class="col-lg-4 col-sm-12">
                            <div class="about_main">
                                <h1 class="follow_text">CONNECT & FOLLOW</h1>
                                <div class="follow_icon">
                                    <ul>
                                        <li><a href="#"><img src="/images/fb-icon-1.png"></a></li>
                                        <li><a href="#"><img src="/images/twitter-icon-1.png"></a></li>
                                        <li><a href="#"><img src="/images/linkedin-icon-1.png"></a></li>
                                        <li><a href="#"><img src="/images/instagram-icon-1.png"></a></li>
                                    </ul>
                                </div>
                            </div>
                        </div>*@
                    </div>
                </div>
            </div>


        }
    </div>


</div>